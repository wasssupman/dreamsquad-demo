namespace Wassup.Tests.EditMode
{
    // 은퇴 — skill-layer-migration unit 8.
    //
    // 이 파일은 `AttackSystem.ApplyConeBreath` 를 리플렉션으로 붙잡아 arm 의 필터 넷
    // (진영 · 통행 층 · 자기 제외 · 부채꼴)을 고정했다. 화염 브레스가 스킬 레이어로
    // 오면서 그 헬퍼가 사라졌고, **불변식은 사라진 게 아니라 두 곳으로 갈렸다**:
    //
    //   · 부채꼴 판정        → `ConeBreathSkillTests` (concrete 가 직접 본다)
    //   · 진영 · 층 · 자기   → 같은 파일 — 이제 후보 질의(`CandidateFilter`)의 몫이라
    //                          「질의가 걸러 주는가」를 통과 결과로 확인한다
    //
    // ⚠ 리플렉션으로 private static 을 붙잡는 그물은 **구현을 옮기는 순간 빨개진다.**
    // 그게 이 그물의 값이기도 했다(조용히 사라지지 않았다). 다만 옮긴 뒤의 자리는
    // 포트 너머라 리플렉션이 필요 없다 — concrete 는 그냥 부를 수 있다.
    //
    // 파일을 지우지 않고 이 표지만 남기는 이유: 커밋 로그를 뒤지지 않고도 「그 그물이
    // 어디로 갔나」를 여기서 읽게 하려는 것이다.
}
