# Object Pipeline Map — 플레이 오브젝트 생성→렌더 커버리지 체계

상태: 완료 2026-07-06 (unit 0, 커밋 `aeccbc3a`) — handoff: `1_handoff_summary.md`

## 목표

플레이 오브젝트(유닛/적/투사체/해저드/VFX 등)의 spec 작성 시 "생성→렌더 파이프라인 정거장"을 매번 한두 개씩 누락하는 반복 사고를 구조적으로 막는다.

방식: **레퍼런스 체크표 문서 1장 + spec 템플릿 필수 섹션 규칙**. 지식을 설명문으로 쌓지 않고, spec README 작성 시점에 대조를 강제한다.

## 검증 질문

"새 플레이 오브젝트 spec 을 쓸 때, 파이프라인 정거장 누락이 spec 단계에서 드러나는가?"

## 작업 단위

| # | 작업 | 문서 | 목적 |
|---|---|---|---|
| 0 | 파이프라인 맵 + 규칙 | `0_pipeline_map_and_rules.md` | `docs/reference/object-pipeline-map.md` 작성 + CLAUDE.md 규칙 2줄 추가 |

## Feature-wide 계약

- 맵 문서는 **대조용 체크표**다. 아키타입별 "정거장 / 코드 앵커 / 확인 포인트" 표만 담고, 시스템 동작 설명·이벤트 필드·코드 흐름 산문은 넣지 않는다. 구현 상세의 source of truth 는 코드.
- 플레이 오브젝트 신규/변경 spec 의 README 에는 **`파이프라인 커버리지` 섹션이 필수**다. 가장 가까운 아키타입 행을 복사하고, 해당 없는 정거장은 빈 칸이 아니라 **`N/A + 이유`** 를 적는다.
- 맵 문서 갱신 트리거는 **구조 변경만**: 새 아키타입, 정거장 추가/제거(새 큐·새 pool), 앵커 파일 이동/개명. 수치·필드·시스템 내부 로직 변경은 갱신 대상이 아니다.
- 갱신 강제 지점: handoff summary 작성 단계에서 "파이프라인 맵 구조 변경 여부" 확인 + spec 작성 시점의 자가 검진(대조 중 어긋남 발견 시 그 자리에서 수정).

## 후속 후보

- spec 파일 트리거 훅 (`docs/spec/**/README.md` Write/Edit 시 커버리지 섹션 리마인더 주입) — 대안 B, A 정착 후 잔여 누락 케이스 확인되면
- two-track-review / critic 체크리스트에 파이프라인 정거장 누락 항목 추가 — 대안 C
