# 24 — 착지 예고를 **점 + 거리**로 (사각 타일 → 원 링)

> 제약 13 미이행분 중 **[높음]**. unit 23 전수 감사가 「**오늘 이미 화면이 거짓말 중**」이라고
> 적어 둔 유일한 항목이다 — 다른 미이행분은 판정끼리의 불일치인데 이것은 **플레이어에게 보이는** 거짓이다.

## 무엇이 틀렸나

궁극기 강습의 **착지 예고**가 `[-N,+N]²` 사각을 통째로 칠했다. 슬램 피해는 unit 4b 이후 **원**이라
**자가 갈려 있었고**, 「예고 셀 = 피해 셀」이라던 주석은 그 시점부터 거짓이었다.

⚠ **다만 그 사각이 «거짓 경고»는 아니었다** (2026-09-07 재검산 — 이 unit 이 한동안 헤드라인으로
쓰던 *「모서리는 안 맞는데 칠해졌다」* 는 **거짓**이다). 그 계산은 대상을 **점**으로 놓은 것이고,
피해 도달은 `N + 0.5 + 대상 몸`이며 방어유닛 몸의 **최솟값이 0.5** 다 — 정대각 칸에 선 유닛은
로스터 4종 **전부 맞는다**(버스터즈 2.828 ≤ 3.0). **틀린 것은 «자» 이지 «칠한 칸» 이 아니었다.**

## 형이 먼저다 — 착지 슬램은 「자리에 떨어지는 것」 (2026-09-07 사용자 결정)

이 unit 은 처음에 **틀린 전제** 위에 지어졌다. unit 23b 가 착지 슬램을 「그 몸이 내리찍는 것」으로
읽어 보스의 `HitRadius` 를 실었고, 예고도 그 몸을 받아 그렸다.

사용자 결정으로 뒤집혔다 — *「이건 운석과 마찬가지로 착지지점(좌표) 기준 거리 N」*.
보스는 **좌표를 지정하고 거기에 내린다**. 지정은 기하가 아니라 귀속이므로 그 몸은 안 붙는다.

| | 값 | 근거 |
|---|---|---|
| 원점 항 | **칸 반폭 0.5** | 자리형 — 좌표에 내린다. 몸이 아니라 «칸 하나를 덮는 도형 보정» |
| 예고의 대상 항 | **0 — 칸을 점으로 본다** | 「중심점 기준 N거리, **몸체 크기 상관없이**」 |
| 피해의 대상 항 | 그 유닛의 몸 | 판정은 언제나 상대의 몸을 잰다(제약 13) |

→ 예고 도달 = **N + 0.5**, 피해 도달 = **N + 0.5 + 대상 몸**.

⚠ **표기에 몸을 끌어들이지 않는다** (2026-09-07 사용자 결정 — *「보스 리프는 중심점을 기준으로
N거리 적용한다고 몸체 크기 상관없이. 운석과 같은 메커니즘」*). 시행착오로 두 번 틀렸다:
① 원점에 **보스 몸**(unit 23b) → ② 대상에 **표준 방어유닛 몸 1.0**(모양을 고치려고). 둘 다
「누구 기준이냐」를 만들고, 그 순간 화면이 유닛마다 다른 규칙을 말한다.

**부수 효과 — 축 뿔이 구조적으로 사라진다.** 대상 항에 1×1 몸(0.5)을 넣었을 때 도달이 **정확히
3.0** 이 되어 축 칸 `(3,0)` 만 «경계 동률»로 들어오고 이웃 `(3,1)=3.16` 은 빠졌다(5×5 에 뿔 4개 —
*「완전한 원이어야지 왜 타원형을 그리지」*). 격자에서 도달이 정수면 언제나 그렇게 된다.
`N + 0.5` 는 **정수가 될 수 없어** 그 동률에 걸리지 않는다.

```
   N=1 (9칸)      N=2 (21칸)      N=3 (37칸)
     ■■■            ·■■■·          ··■■■··
     ■■■           ■■■■■         ·■■■■■·
     ■■■           ■■■■■         ■■■■■■■
                    ■■■■■         ■■■■■■■
                    ·■■■·          ■■■■■■■
                                    ·■■■■■·
                                     ··■■■··
```

반경 1 의 3×3 이 온전한 것은 **의도**다 — 「대각 인접도 사거리 1」은 이 게임의 오래된 계약이고,
`0.5` 라는 도형 보정항이 존재하는 이유 그 자체다(unit 4b).

⚠ **`flightTime == 0`(즉발) 을 형의 판별로 쓰지 말 것.** 자폭·시체폭발과 배선이 같아 보이지만
그쪽은 폭심이 «그 몸» 이고 착지 슬램은 «지정한 좌표» 다.

## 변경 대상

- `Scripts/Skills/SkillMath.cs` — 진입점 **4개 유지**. `ReachFromUnitToCell` 은 신설했다가 같은 날 철거.
- `Scripts/Bridge/BattleBridge.cs` — `BuildZoneCells` 가 원 술어(`ReachFromCell`)를 지난다. 스캔 상한 추가.
- `Scripts/Bridge/BattleBridge.UltimateLeap.cs` — 예고가 **보스 몸을 읽지 않는다**.
- `Scripts/Battle/Combat/UltimateLeapSystem.cs` · `Scripts/Bridge/BattleBridge.BossLeap.cs` —
  슬램 생산자 2곳이 `originBodyRadius = 0`(자리형).
- 테스트 — `TileAoeTests`(4, 그중 1건은 반경 1~5 파라미터) · `ReachEntryPointGuardTests`(2).

## 실제로 바뀐 것 (라이브 저작 기준)

슬램을 저작한 적은 **짱쎈 하나**(`Enemy_Boss_Jjangssen.asset` — 강습 반경 2 · 몸 0.5)뿐이고,
그 몸이 정확히 **0.5 = 칸 반폭**이라 **피해 숫자는 하나도 안 바뀐다.** 바뀐 것은 예고 도형이다.

| | 옛 사각 | 새 원 |
|---|---|---|
| 칠해지는 칸 | 25 | **21** |
| 늘어난 칸 | — | **0** (새 집합이 옛 집합의 부분집합이다) |
| 빠진 칸 | — | 모서리 4개 `(±2,±2)` |

### 순효과 — 세 상태 비교

라이브 N=2 · 앵커 전수. **무경고** = 맞는데 화면이 안 알려줌 · **과경고** = 안 맞는데 알려줌.

| | 이전(사각 타일 25칸) | 중간(원 타일 21칸) | **현재(원 링)** |
|---|---|---|---|
| **무경고** | 24 | 32 ↑ | **0** |
| 과경고 | 16 | 8 ↓ | **0** |

> ⚠ **중간 상태를 «개선» 으로 기록하지 말 것.** 칸 열거를 원으로 바꾼 것만으로는
> **「안전한 오차를 위험한 오차로 바꾼 것」**이었다(무경고 24→32). 자는 옳아졌는데
> 순 정보량은 나빠졌다 — 두 문장을 섞지 말 것.
>
> **링으로 바꾸고 나서야 둘 다 0 이 된다.** 오차의 정체가 «반경»이 아니라 **매체의 양자화**였기
> 때문이다. 링에서는 「그림자가 링에 닿나」가 판정식과 **동치**라 오차가 정의상 존재하지 않는다.

⚠ 원인은 코드가 아니라 **감사 방법**이다. unit 23·24 가 예고와 피해를 **「대상 몸 0」 기준**으로
비교했는데 그런 방어유닛은 로스터에 없다. 이 spec 이 세 번 반복한 실패의 **네 번째**다 —
*「몸이 상수 가정에서 데이터로 승격됐는데, 그 값을 «쓰던» 곳이 아니라 «가정하던» 곳이 안 고쳐졌다」*.

## 왜 링인가 — 매체가 원인이었다

**원인은 몸도 반경도 아니라 «매체» 다** (2026-09-07 재진단).

운석은 **두 항이 같은 숫자**(`N + 0.5` · 대상 몸 미포함 — `PinCenteredRange`, 사용자 결정
2026-09-02 D6)인데 이 문제가 없다. 매체가 **SDF 링**(`SetAreaRange`)이라 「**내 그림자가 링
가장자리에 닿나**」로 읽히고, 그 판독이 `|Δ| ≤ N + 0.5 + 대상 몸` 과 **수학적으로 동치**이기 때문이다.
그래서 표기가 몸을 몰라도 플레이어는 **자기 유닛의 그림자**로 몸을 읽는다 — 그 계약이
`TilemapMapView.SetAreaRange` 헤더에 이미 문장으로 있다.

착지 예고만 **이산 타일 채움**(`SetTelegraphCells`)이라 그 어포던스가 없다. 링 가장자리에 걸치는
띠가 「칠해짐/안 칠해짐」 **이진값으로 접혀** 통째로 «안 칠해짐» 쪽에 떨어지고, 짝수 폭 유닛의
발밑이 칸 **경계**에 있어(`FootOffset` = ((W−1)/2, 0)) 격자가 그것을 표현할 방법이 원리적으로 없다.

> ⚠⚠ **그래서 고친 것은 반경이 아니라 매체다.** 무경고 32 는 «타일 채널의 양자화 오차» 이지
> «규칙의 오류» 가 아니었다. 반경을 몸에 맞춰 32 를 없애려는 시도가 **두 번** 있었고
> (unit 23b 보스 몸 · 그 뒤 「표준 방어유닛 몸」) 둘 다 「누구 기준이냐」를 만들었다.
> **세 번째를 하지 말 것** — 매체를 바꾸면 반경을 건드리지 않고 오차가 0 이 된다.

라이브 저작(반경 2) 전수 — **점유 칸이 하나도 안 칠해졌는데 맞는** 앵커(= 위 무경고 32 의 내역):

| | 2×2 (25종) | 배스티온 3×2 | 캐논 2×3 | 버스터즈 1×2 |
|---|---|---|---|---|
| 맞는데 예고 밖 | 8 | **11** | 8 | 5 |
| 안 맞는데 칠함 | 0 | 0 | 6 | 2 |

⚠ **반경을 몸에 맞추는 방향으로 고치지 말 것.** 그러면 「누구의 몸이냐」가 생기고, 운석과
두 항이 달라져 「같은 메커니즘」이 오히려 깨진다(운석의 피해도 대상 몸을 더한다 — 같은
`SkyFall`+`TileAoe` 팔이다). 고칠 것은 **매체**다.
`TileAoeTests.LandingTelegraph_DoesNotKnowBodies_SoBodiedUnitsAreHitOutsideIt` 가 표본 둘
(배스티온 앵커 `(-1,3)` · 2×2 앵커 `(2,2)`)을 핀으로 박아 뒀다 — 수가 바뀌면 규칙이 바뀐 것이다.

## 완료 기준

- [x] 예고가 판정과 **같은 본체**를 지난다(`ReachFromCell`).
- [x] **형이 자리형이다** — 예고가 보스 몸을 안 읽고, 생산자 2곳이 `originBodyRadius = 0` 을 싣는다
      (`LandingSlam_IsAPlaceForm_InTelegraphAndBothProducers` — 세 파일을 한 단언이 묶는다).
- [x] **표기가 몸을 모른다** — 대상 항 0. 소스 가드가 `const float targetR = 0f` 를 고정한다.
- [x] **모양이 원이다 — 반경 1~5 전부** 축 뿔이 없다(도달이 정수가 될 수 없다).
- [x] **모서리는 반경 2 부터 빠지고, 반경 1 의 3×3 은 온전하다**(대각 인접 계약).
- [x] **예고 = 「대상을 점으로 본 피해」** — 8×8 전수 대조.
- [x] **대가가 단언으로 있다** — 몸 있는 유닛이 예고 밖에서 맞는 표본 2건이 박혀 있다.
- [x] **되돌아가지 못하게** — 소스 가드가 사각 열거 부활·대상 항 변조를 잡는다(윈도는 다음 선언까지).
- [x] **점 + 거리로 전환** — 칸 열거를 통째로 철거하고 **운석과 같은 함수**(`PinSkillTelegraph`)를
      부른다. 반경은 그 함수의 성질(`N + 칸 반폭`)이라 호출부가 조립하지 않는다.
- [x] **그림자↔링 동치를 단언으로** — 몸 5종(0·0.25·0.5·1.0·1.5) × 거리 0~8 전수로
      「그림자가 링에 닿는다 ⟺ 피해 판정」을 고정(`ShadowTouchingTheRing_IsExactlyTheDamagePredicate`).
- [x] **옛 무경고 표본이 링에서는 읽힌다** — 배스티온 `(-1,3)` · 2×2 `(2,2)`.
- [x] **칸 열거가 되살아나지 않는다** — 소스 가드가 `SetTelegraphCells`·`BuildZoneCells` 부활을 잡는다.
      ⚠ 초판 가드는 **vacuous** 였다(면제 절 `&& !view.Contains("되돌리지 말 것")` 이 같은 파일
      주석에 걸려 **항상 통과**). 주석을 제거한 «코드만» 스캔하도록 고쳤다 — 이 레포에서 세 번째다.
- [x] **예고는 «전용» 채널이다** — 한 번 운석의 공유 `_rangeOwner` 채널(`PinSkillTelegraph`)로
      그렸다가 리뷰가 CRITICAL 로 잡았다. 그 채널은 단일 owner set/clear 라 예고 2초 동안
      **배치 프리뷰와 예고가 서로를 지우고**(배치가 이기면 재페인트 경로가 없어 남은 시간 전부
      무경고 — 탭 배치 peek 는 매 프레임 훔쳐서 1프레임만 산다), **운석 예고와도 서로를 지웠다**.
      → `TilemapMapView.SetTelegraphRing`/`ClearTelegraphRing` 전용 링 인스턴스로 분리.
      가드가 예고에서 `PinSkillTelegraph`·`ClearSkillTelegraph`·`SetAreaRange`·`SetRangeOwner` 를 금지한다.
      ⚠ **철거 대상 코드의 헤더가 이 금지를 이미 적어 뒀는데 안 읽었다** — 「소비처 0 이 된 코드가
      왜 그렇게 생겼는지」를 읽지 않고 지운 것이 이 라운드의 실제 결함이다.
- [x] **색이 갈린다** — 예고는 `landingTelegraphColor`(타일 시절과 같은 저작 필드), 사거리·조준 링은
      라임. 공유 채널을 쓰던 동안은 **보스 슬램 경고가 플레이어 자기 조준 링과 동색**이었다.
- [x] **링 기하는 한 곳이 소유한다** — `PlaceRing`. 실측으로 얻은 값(`CellToLocalInterpolated` +
      접지 리프트)이라 두 벌이 되면 한쪽만 조용히 어긋난다.
- [ ] Play 육안: 보스 강습 예고가 **착지점 중심의 원 하나**로 뜨는지.

## 남은 죽은 코드 — 사유를 정정한다

`TilemapMapView` 의 **타일** 예고 채널(`SetTelegraphCells`/`ClearTelegraphCells`/`_telegraphCells`/
`_telegraphTilemap`/`EnsureTelegraphTilemap`/`ApplyTelegraphTint`)은 producer 0 이다.

⚠ 커밋 메시지에 *「씬의 serialized Tilemap 참조를 건드려야 해서」* 라고 적었는데 **거짓**이다 —
`_telegraphTilemap` 은 `private Tilemap` 이고 `EnsureTelegraphTilemap` 이 `new GameObject` 로
**런타임 생성**한다. 실제 접점은 셋뿐이다:

| 접점 | 상태 |
|---|---|
| `landingTelegraphColor` (SerializeField, 씬 값) | **계속 쓴다** — 링 색이 이 필드다 |
| `TileSetData.telegraphTile` | **다른 소비처가 있다**(`:1367`) — 이 채널만의 것이 아니다 |
| `SetRendererSorting(_telegraphTilemap, …)` (`:342`) | 채널과 함께 지운다 |

즉 은퇴 비용은 **뷰 파일 안에서 닫힌다**. 다음 작업으로 뺀 것은 비용이 아니라 **범위** 때문이고,
그때까지 뷰에 남은 API 를 다음 사람이 되쓰는 것은 **아무 그물도 막지 않는다**(소스 가드는
`BattleBridge*.cs` 만 본다).

