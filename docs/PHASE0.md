# Phase 0 — 실시간 디펜스 루프

> 본 문서는 `PRD.md`와 `TRD.md`를 전제로 작성되었다. 기술 스택·ECS 경계·맥락 분리·금지 패턴·추상화 규칙은 TRD 섹션 2~5에 있으며 본 문서에서 반복하지 않는다. 본 문서는 **Phase 0의 What과 Done만 정의**한다. How(기술적 설계·클래스 구조·테스트 구성)는 에이전트 구현 재량에 일임한다.

---

## 0. Phase 0의 존재 이유

### 검증 목표

> **"이 게임의 실시간 디펜스 루프가 재미있는가, 최소한 작동하는가"**

Phase 0는 드래프트도, 스킬도, 인접 시너지도 없는 **가장 헐벗은 실시간 디펜스 상태**를 만든다. 이 상태에서 플레이어가 "유닛이 밀려오고, 내가 배치하고, 막혀나가는" 감각을 받는지 본다. 받으면 이 게임의 근본 재미가 존재한다는 뜻이고, 안 받으면 나머지 Phase가 의미 없다.

본 Phase는 PRD의 검증 가설 H1~H4 중 **H2(실시간 긴장감)와 H3(패배의 자기 귀인)**을 선제 검증하는 의미가 있다. H1(드래프트 학습 신호)은 Phase 1(드래프트 도입) 이후, H4(아키텍처 타당성)는 Phase가 진행되면서 누적 검증된다.

### Phase 0가 하는 것 / 안 하는 것

**Phase 0가 하는 것:**
- 정해진 타임라인에 따라 공격 유닛이 경로를 따라 종점까지 이동
- D&D 제스처로 배치 타일에 터치/마우스가 도달하면 **랜덤한 방어 유닛**이 배치됨
- 방어 유닛이 공격 유닛을 자동 공격
- 경로 끝에 공격 유닛 N마리 도달 시 패배
- 공격 유닛 전멸 시 승리
- 승패 결과를 화면에 표시 + 로그 파일에 남김

**Phase 0가 하지 않는 것:**
- 드래프트 (Phase 1)
- 스킬 (Phase 2)
- 배치 시 효과 / 인접 시너지 (Phase 3)
- 코스트 시스템 (Phase 4 또는 그 이후로 유보)
- 유닛 종류 선택 (랜덤 배치)
- UI 장식, 결과 화면 화려함, 통계 패널
- 튜토리얼, 설정, 사운드
- 모바일 특화 최적화 (다만 Android 빌드는 동작해야 함)

**주의**: "배치 시 랜덤"은 Phase 1에서 "드래프트한 7종 중 사용자가 고른 것"으로 교체된다. 지금 랜덤으로 두는 이유는 Phase 0의 검증 목표가 "실시간 루프 자체"이지 "선택 결정"이 아니기 때문이다.

---

## 1. Phase 0의 게임 흐름

플레이어가 빌드를 실행하면 다음이 일어난다. 메인 메뉴, 토너먼트 선택, 드래프트 UI는 **없다**.

```
[빌드 실행]
  ↓
[맵 + 경로 + 배치 타일 표시]
  ↓
[시작 버튼 또는 자동 시작 — 판단은 에이전트 재량]
  ↓
[타임라인에 따라 공격 유닛이 경로 시작점에 등장]
  ↓
[플레이어는 배치 타일을 터치/드래그하여 방어 유닛 배치]
  ↓
[방어 유닛은 자동으로 사거리 내 공격 유닛 공격]
  ↓
[공격 유닛은 경로 따라 종점 이동, 이동 중 공격받으면 체력 감소, 0 이하 시 소멸]
  ↓
[종점 도달 N마리 ⇒ 패배 / 공격 유닛 전멸 ⇒ 승리]
  ↓
[결과 화면 (최소: "VICTORY" / "DEFEAT" 텍스트 + "다시" 버튼)]
```

---

## 2. Phase 0 콘텐츠 스펙

### 2.1 맵

- **20×10 복셀 그리드** (PRD 섹션 2.5 준수)
- 타일 종류 3가지: **경로 타일 / 배치 가능 타일 / 장애물 타일**
- 맵 구성은 고정 1장. 프로토타입 내내 이 맵 한 장을 재사용한다.

### 2.2 경로

**2~3개 경로**를 가진다. 경로의 성격은 각각 명확히 구분되어야 한다:

- **경로 A — 직선**: 가장 단순한 경로. 디버깅과 기본 동작 검증용
- **경로 B — 곡선 또는 L자**: 방향 전환이 있는 경로. 유닛의 경로 따라가기 로직 검증
- **경로 C — 분기 또는 우회** (선택): 다른 방식의 경로. 둘로 충분하면 생략 가능

각 경로는 시작점(등장 지점)과 종점(패배 지점)을 가진다. 경로 간 교차 여부는 에이전트가 맵 디자인상 편한 쪽을 선택한다.

### 2.3 공격 유닛 (2~3종)

각 유닛은 기본 스탯을 가진다: **체력 / 이동 속도 / (단순) 공격력**. 이 단계에서 공격 유닛의 공격 행동은 "경로 따라가기"일 뿐이며 방어 유닛을 공격하지 않는다 — 방어 유닛이 일방적으로 공격 유닛을 처리한다. (이 단방향성은 Phase 0의 단순성을 위한 것이며 이후 Phase에서 바뀔 수 있음.)

**유닛 A — 탱커**
- 체력 높음, 이동 속도 느림
- 역할: "잘 안 죽는 적"이라는 긴장감

**유닛 B — 일반**
- 중간 체력, 중간 속도
- 역할: 기본 단위

**유닛 C — 빠른 유닛** (선택)
- 낮은 체력, 빠른 속도
- 역할: "놓치면 종점 빨리 닿음"의 긴장감

세 유닛 중 최소 **2종**이 구현되어야 한다. 3종은 권장.

### 2.4 방어 유닛 (2~3종)

**유닛 A — 근접**
- 짧은 사거리, 중간 공격력, 빠른 공격 속도

**유닛 B — 원거리**
- 긴 사거리, 낮은 공격력, 느린 공격 속도

**유닛 C — 특수** (선택)
- 예: 광역 공격 (스플래시) 또는 느리지만 높은 피해
- 2종으로 충분하면 생략 가능

세 유닛 중 최소 **2종**이 구현되어야 한다. 3종은 권장.

**공격 행동**: 사거리 내에 들어온 공격 유닛을 자동 타겟팅하여 공격한다. 타겟팅 규칙(가장 가까운 적 / 가장 앞선 적 / 가장 체력 낮은 적)은 에이전트 재량이되, 일단 하나로 고정한다.

### 2.5 배치

- 플레이어는 **배치 가능 타일**을 터치(모바일) 또는 클릭/드래그(에디터)하여 방어 유닛을 배치
- 배치되는 유닛의 종류는 **랜덤**. 플레이어는 선택하지 않는다.
- 타일당 1종만 배치 가능 (PRD A안 준수)
- 배치 후 제거/이동 불가

**배치 개수 제한**: 없음. 원하는 만큼 배치 가능.
**코스트**: 없음. Phase 0는 코스트 시스템 이전 단계.

> **노트**: "무제한 배치 + 랜덤 유닛"은 Phase 0가 재미없을 위험을 안고 있다. 하지만 이 단계에서 "재미있는 제약"을 넣기 시작하면 Phase 0의 범위가 부풀어 오른다. 제약의 부재가 오히려 "현재 루프가 얼마나 지루한지"를 드러내는 장치로 쓴다. 지루하면 Phase 1(드래프트 선택)과 Phase 4(코스트 제약)가 의미 있다는 신호다.

### 2.6 공격 타임라인 (공격덱) 스키마

Phase 0 공격덱은 다음 스키마로 정의된다. 구체 포맷(ScriptableObject / JSON)은 에이전트 재량:

```
AttackDeck {
  name: string
  spawns: [
    {
      trigger_time_sec: float     // 판 시작으로부터 초
      unit_type: UnitTypeId       // "tanker" | "basic" | "fast" 등
      path_id: PathId             // "A" | "B" | "C"
    },
    ...
  ]
  defeat_condition: {
    type: "enemy_reached_goal"
    count: int                    // 종점 도달 허용 수. 초과 시 패배
  }
}
```

Phase 0에서는 **2~3개의 고정 AttackDeck**을 미리 만들어둔다. 빌드 실행 시 에이전트가 지정한 방식(고정 / 랜덤 / 순환)으로 하나가 선택된다.

### 2.7 승패 조건

- **승리**: 타임라인의 모든 공격 유닛이 소멸됨
- **패배**: 경로 종점에 도달한 공격 유닛 수가 AttackDeck의 `defeat_condition.count` 초과
- **타이머**: Phase 0에서는 3분 타이머를 **두지 않는다**. 타임라인이 끝날 때까지 판이 진행된다. (3분 타이머는 실시간 긴장감 검증에 중요하지만, Phase 0는 "재미 있음/없음"의 1차 판단이 목표이므로 타이머 없이 먼저 확인한다. 타이머 도입은 Phase 0 검증 직후 재검토.)

### 2.8 결과 화면

**최소 구성**:
- "VICTORY" 또는 "DEFEAT" 텍스트
- "다시 시작" 버튼 (같은 판을 다시 플레이)
- (선택) "다른 공격덱으로" 버튼

결과 화면은 꾸밀 필요 없다. 기능만 동작하면 된다.

### 2.9 로깅 (최소)

Phase 0에서도 로깅은 첫 빌드부터 존재해야 한다. PRD 섹션 2.11을 기준으로 하되, Phase 0 단계에 해당하는 필드만 채운다:

```json
{
  "session_id": "...",
  "phase": "phase0",
  "timestamp": "...",
  "attack_deck_id": "...",
  "placements": [ {"unit_type": "...", "tile": [x, y], "time": ...} ],
  "result": { "outcome": "victory|defeat", "duration_sec": ..., "enemies_reached_goal": ... }
}
```

JSON 파일로 로컬 저장. 경로와 포맷은 에이전트 재량이되, Phase 1에서도 확장 가능한 구조여야 한다.

---

## 3. 종료 조건 (Done Criteria)

Phase 0는 다음이 **모두** 이진 통과해야 Phase 1로 이동한다.

### 3.1 기능 이진 체크 (작업 순서)

아래는 **의존 관계에 따라 순서대로 정렬된** 작업 목록이다. 각 작업은 선행 작업이 통과한 상태에서 시작하며, 완료 기준은 "해당 체크박스가 true가 되는 것"이다. 에이전트는 미체크 작업 중 **가장 위 항목**부터 진행한다.

**작업 수행 원칙**:
- 한 번에 한 작업만 구현한다. 작업 완료 전에 다음 작업을 미리 건드리지 않는다.
- 각 작업 완료 시 사용자에게 통과 여부 확인을 요청한다.
- 작업 중 TRD 섹션 4(금지 패턴)를 위반하는 유혹이 생기면 정지하고 질문한다.
- 작업 간 내린 기술 결정은 `phase0-decisions.md`에 한 줄씩 누적 기록한다.

---

**[P0-01] 프로젝트 부트스트랩**
- [x] Unity 6 프로젝트가 Entities 1.4.x (Unity 6 호환) 패키지와 함께 빌드됨
- 내용: Unity 6 새 프로젝트 생성, TRD 섹션 1.2의 필수 패키지 전부 설치, 최소 빈 씬 1개, Android 빌드 타겟 설정, `.gitignore` 정리
- 선행: 없음
- 완료 확인: 에디터 플레이 + Android 빌드 양쪽 무오류
- 비고: Android 빌드 검증은 사용자 지시(`android 검증 패스`)로 P0-13까지 유보. 에디터 컴파일 무오류 확인 완료. 상세는 `phase0-decisions.md` P0-01 섹션 참조.

**[P0-02] 맵 & 그리드**
- [x] 에디터 플레이 모드에서 맵(20×10 그리드, 2~3개 경로)이 표시됨
- 내용: 20×10 그리드 생성, 타일 타입 3종(경로/배치/장애물) 구분 표시, 2~3개 경로 데이터 정의(waypoint 또는 등가)와 시각화
- 선행: P0-01
- 완료 확인: 플레이 시 맵과 경로가 눈으로 구분 가능

**[P0-03] 로깅 기반**
- [x] 판 종료 시 JSON 로그 파일이 로컬에 기록됨 (초기 골격만)
- 내용: 섹션 2.9의 JSON 스키마에 맞춰 Logger 구현. 이 시점에는 "판 시작/종료 시각"만 기록되어도 충분. 로깅은 **기능보다 먼저** 기반을 깐다 (TRD 섹션 3.4).
- 선행: P0-01
- 완료 확인: 판 시작/종료 시 JSON 파일이 지정 경로에 생성됨. P0-02와 병행 가능하나 P0-04 전에 완료되어야 함.

**[P0-04] 공격 유닛 1종 — 이동**
- [x] 타임라인에 따라 공격 유닛 1종이 경로 시작점에 등장
- [x] 해당 유닛이 지정된 경로를 따라 종점까지 이동
- 내용: ScriptableObject로 공격 유닛 1종 정의(탱커 또는 일반), AttackDeck 스키마 1개 정의(단일 유닛 고정 스폰), ECS 시스템으로 경로 따라가기 구현. `BattleBridge`를 통해 MonoBehaviour ↔ ECS 경계 확립.
- 선행: P0-02, P0-03
- 완료 확인: 한 유닛이 시작점에서 종점까지 이동

**[P0-05] 종점 도달 + 패배 판정**
- [x] 종점 도달 수가 임계값 초과 시 "DEFEAT" 결과 표시
- 내용: 종점 도달 이벤트를 ECS에서 감지 → `BattleBridge`로 전달 → MonoBehaviour 쪽에서 카운트 및 패배 판정. AttackDeck의 `defeat_condition.count` 사용.
- 선행: P0-04
- 완료 확인: 종점 도달 카운트가 N 초과하면 DEFEAT 화면 전환

**[P0-06] 배치 입력**
- [x] 배치 가능 타일을 터치/클릭하면 랜덤한 방어 유닛이 생성됨 (방어 유닛 1종 기준)
- 내용: 터치/마우스 입력 처리(MonoBehaviour), 타일 히트 테스트, 배치 가능 타일인지 검증, ScriptableObject로 방어 유닛 1종 정의, 배치 시 `BattleBridge`를 통해 ECS 엔티티 생성. 타일당 1종 제약 준수.
- 선행: P0-04
- 완료 확인: 배치 가능 타일 클릭/터치 시 방어 유닛이 생성되고 시각적으로 표시됨

**[P0-07] 자동 공격 + 유닛 소멸**
- [x] 방어 유닛이 사거리 내 공격 유닛을 자동 공격
- [x] 공격 유닛의 체력이 0 이하가 되면 소멸
- 내용: ECS 시스템에서 타겟팅 규칙 1종 고정(에이전트 재량), 공격 쿨다운, 데미지 적용, 체력 0 이하 시 엔티티 파괴.
- 선행: P0-06
- 완료 확인: 배치된 방어 유닛이 자동으로 공격 유닛을 공격해 죽임

**[P0-08] 승리 판정**
- [x] 모든 공격 유닛 소멸 시 "VICTORY" 결과 표시
- 내용: ECS 시스템이 "모든 스폰 완료 + 살아있는 공격 유닛 0"을 감지 → `BattleBridge`로 전달 → VICTORY 화면.
- 선행: P0-07
- 완료 확인: 모든 적을 처치하면 VICTORY 화면

**[P0-09] 결과 화면 + 재시작**
- [x] "다시 시작" 버튼으로 같은 판 재시작 가능
- 내용: VICTORY/DEFEAT 화면에 재시작 버튼, 버튼 클릭 시 ECS 월드 정리 + 판 초기화. ECS 월드 재생성은 `BattleBridge`의 책임(TRD 섹션 2.4).
- 선행: P0-05, P0-08
- 완료 확인: 재시작 후 같은 판이 깨끗한 초기 상태로 다시 진행

**[P0-10] 한 판 완주 + 로그 기록 확장**
- [x] 최소 1개의 AttackDeck에 대해 승리와 패배 양쪽이 재현 가능
- [x] 판 종료 시 JSON 로그 파일에 섹션 2.9의 필드가 채워짐
- 내용: P0-03의 로깅 골격에 placements / result / duration 등 실제 데이터 채우기. 판을 일부러 이기는 조건과 지는 조건을 각각 만들어 양쪽이 모두 재현 가능함을 확인.
- 선행: P0-09
- 완료 확인: 승리와 패배를 의도적으로 재현할 수 있고, JSON 로그에 정확히 기록됨

**[P0-11] 공격 유닛 종류 확장**
- [x] 타임라인에 따라 2~3종의 공격 유닛이 경로 시작점에 등장 (섹션 2.3)
- 내용: 섹션 2.3의 공격 유닛 B(일반)와 C(빠른 유닛, 선택)를 추가. AttackDeck 스키마에 여러 종류를 섞을 수 있도록 확장.
- 선행: P0-10
- 완료 확인: 한 판에 2~3종의 공격 유닛이 섞여 등장

**[P0-12] 방어 유닛 종류 확장**
- [x] 방어 유닛 2~3종이 구현되어 배치 시 랜덤으로 선택됨 (섹션 2.4)
- 내용: 섹션 2.4의 방어 유닛 B(원거리)와 C(특수, 선택)를 추가. 배치 시 2~3종 중 랜덤 선택.
- 선행: P0-11
- 완료 확인: 배치 시 여러 방어 유닛이 랜덤 생성되고 각자 다르게 공격

**[P0-13] Android 실기기 검증**
- [ ] Android 실기기 빌드가 동작 (터치 배치 동작)
- 내용: Android APK 빌드, 실기기 설치, 전체 루프 플레이 가능 확인. 터치 입력이 에디터의 마우스 입력과 동등하게 동작.
- 선행: P0-12
- 완료 확인: Android 기기에서 한 판 끝까지 플레이 가능

---

**작업 완료 시점**: 위 13개 작업의 모든 체크박스가 true가 되면 섹션 3.2(아키텍처 이진 체크)를 검증하고, 통과하면 섹션 3.3(주관 평가 게이트)을 진행한다.

### 3.2 아키텍처 이진 체크

TRD 섹션 2(아키텍처 경계)와 섹션 3(추상화 규칙)을 준수했는가.

**경계 준수**
- [x] 공격 유닛 이동·체력·소멸 로직이 ECS 시스템에 있다
- [x] 방어 유닛의 타겟팅·공격·데미지 적용 로직이 ECS 시스템에 있다
- [x] UI / 터치 입력 / 결과 화면은 MonoBehaviour에 있다
- [x] `BattleBridge`(또는 동등한 단일 클래스)가 MonoBehaviour ↔ ECS 통신의 유일한 창구이다
- [x] `BattleBridge` 외부의 MonoBehaviour 코드에 `EntityManager` / `World.DefaultGameObjectInjectionWorld` 직접 호출이 없다
- [x] SubScene이 사용되지 않았다
- [x] 네트워크 코드가 전혀 없다

**맥락 분리 (TRD 섹션 2.5)**
- [x] `Battle/` 하위에 `Units/`, `Movement/`, `Combat/`, `Effects/` 폴더가 존재한다 (Effects는 Phase 0에서 자리만)
- [x] Component는 소유 맥락이 명확하다 — 다른 맥락이 해당 Component를 **직접 수정**하지 않는다
- [x] 맥락 간 통신은 Component 직접 수정이 아니라 **Buffer 또는 NativeQueue** 기반이다
- [x] Effects 맥락은 Phase 0에서 구현이 없거나 최소 구조(폴더 + placeholder)만 있다

**추상화 규칙 (TRD 섹션 3)**
- [x] 상속 계층이 2단계를 넘지 않는다
- [x] 구현체가 1개뿐인 인터페이스가 없다
- [x] "나중을 위한" 미사용 확장 포인트가 없다
- [x] 유닛·공격 패턴 수치가 ScriptableObject에 있으며 하드코딩되지 않았다

### 3.3 주관 평가 (게이트)

위 이진 체크를 모두 통과해도 Phase 1로 가기 전에 다음을 팀 내부에서 판단한다:

- **3~5명이 Phase 0 빌드를 플레이했다**
- 플레이어가 "유닛이 밀려오고 있다"는 실시간 감각을 받았다 (설문 또는 인터뷰)
- 플레이어가 **스스로 "더 좋게 막으려면 뭐가 필요한가"를 자발적으로 말했다** — 이 발언이 Phase 1 이후 도입할 시스템(드래프트/스킬/인접 효과)의 동기를 증명한다
- 플레이어가 패배했을 때 **"어디서 놓쳤는지"를 언어화할 수 있었다** (H3의 선제 검증)

주관 평가에서 "재미있지도 않고 개선 욕구도 안 생긴다"는 신호가 다수면, **Phase 1로 넘어가기 전에 Phase 0의 밸런스 튜닝 또는 범위 조정**을 먼저 해야 한다. 재미없는 루프 위에 드래프트·스킬·시너지를 쌓아도 결과는 "복잡한데 재미없는 루프"가 될 뿐이다.

---

## 4. 에이전트 자율 결정 영역

본 문서는 "무엇을"과 "완료 조건"만 정의하고, 다음은 **구현 에이전트가 자유롭게 결정**한다. 에이전트가 결정한 내용은 Phase 0 완료 시 별도 문서(`phase0-decisions.md` 권장)에 기록하여 Phase 1 작성에 참고한다.

- 프로젝트 폴더 구조, 어셈블리 분리 여부
- 씬 구성 (단일 씬 / 멀티 씬)
- ECS Component 세부 설계 (이름, 필드 구성, 분리 단위)
- ECS System 세부 설계 (System 개수, 의존 순서, SystemGroup 구성)
- `BattleBridge`의 구체 클래스명과 책임 분할 (단일 클래스로 충분한지, 보조 클래스 필요한지)
- Baker 사용 여부 (ScriptableObject → Entity 변환 방식)
- 타겟팅 규칙 (가장 가까운 적 / 가장 앞선 적 / 기타)
- 경로 표현 방식 (waypoint 배열 / 스플라인 / 기타)
- 그리드 좌표계 구현 (int2 / Vector2Int / 커스텀)
- 유닛 비주얼 (Primitive / 단순 스프라이트 / 기타 — 기능 구분만 되면 됨)
- UI 프레임워크 (UGUI / UI Toolkit — 단, 일관성 유지)
- 테스트 전략 (EditMode 단위 테스트 / PlayMode 통합 테스트의 비율)
- 로그 파일 경로, 파일명, 회전 정책

**결정 원칙**: 애매하면 단순한 쪽. Phase 0는 검증용이지 최적 아키텍처 설계용이 아니다. 단, PRD 섹션 1.1.1 / 6.1 / 6.2의 경계·원칙·금지 패턴은 반드시 준수.

---

## 5. Phase 0 종료 시 에이전트에게 요청하는 산출물

- 동작하는 Unity 6 프로젝트 (에디터 + Android 빌드 가능 상태)
- 최소 1개의 EditMode 테스트와 1개의 PlayMode 테스트 (구체 범위는 에이전트 재량)
- `phase0-decisions.md` — 위 4번에서 에이전트가 내린 결정과 그 근거를 짧게 기록
- Phase 1에서 재활용될 핵심 타입(특히 `BattleBridge` 시그니처)의 정리

---

## 6. Phase 0 이후 Phase 순서 (참고용)

본 문서는 Phase 0만 상세화하지만, 작성 당시 합의된 전체 Phase 순서는 다음과 같다. Phase 1 이후 각 Phase의 상세 문서는 **직전 Phase 완료 후** 작성한다.

1. **Phase 0 — 실시간 디펜스 루프** (본 문서)
2. **Phase 1 — 드래프트** (10→7픽 도입, 랜덤 배치 제거)
3. **Phase 2 — 스킬** (스킬 2종, 코스트 시스템 도입 여부 이 시점에 결정)
4. **Phase 3 — 배치 시 효과 / 인접 시너지** (유닛 특성의 깊이 부여)
5. **Phase 4 — 그 외 마무리** (3분 타이머, 더미 봇 스코어, 결과 화면 비교, 측정 프로토콜)

이 순서는 **"가장 근본적인 재미"부터 "가장 파생적인 시스템"까지**의 순서이며, 각 Phase는 직전 Phase의 검증을 전제로 한다. 앞 Phase가 실패하면 뒷 Phase는 무의미하다.

---

## 7. 구현 결과 스냅샷 (2026-04-19)

Phase 0는 현재 구현 완료 상태다. 확정/구현된 세부 결정은 과거 `phase0-decisions.md` 에 기록되었고, 본 문서가 Phase 0 스펙의 단일 출처다.

- Unity 프로젝트는 Universal 3D 기반으로 초기화되었고, 전투 시뮬레이션은 `BattleBridge` 를 유일한 MonoBehaviour ↔ ECS 창구로 사용한다.
- 좌표계는 타일 정수 좌표(`Vector2Int`)와 world XZ 평면을 매핑하는 단순 구조다.
- `AttackDeck`, `MapData`, `AttackUnitData`, `DefenderUnitData` ScriptableObject 기반 데이터 흐름이 구축됐다.
- 공격 유닛 이동, 방어 유닛 배치, 자동 공격, 데미지 적용, 승패 판정, Restart, JSON 로그가 구현됐다.
- Android 실기기 검증은 이후 Phase 검증으로 이월되었고, Editor 기준 루프는 후속 Phase의 기반으로 사용 중이다.

**문서 버전**: v1.0 (구현 스펙 통합)
**상태**: 구현 완료. Android 실기 검증은 후속 residual 검증으로 관리.
